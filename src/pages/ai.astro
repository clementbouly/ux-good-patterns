---
import PageLayout from "@/layouts/PageLayout.astro";
import { AiContextActions } from "@/components/AiContextActions";
import { CopyableUrl } from "@/components/CopyableUrl";
---

<PageLayout title="AI Context | The Good, The Bad and The UX" description="Use UX best practices as context for your AI coding assistant">
  <div class="mx-auto max-w-3xl">
    <h1 class="text-3xl font-bold mb-2">AI Context</h1>
    <p class="text-muted-foreground mb-8">
      Give your AI coding assistant UX superpowers
    </p>

    <div class="rounded-xl border bg-card p-6 md:p-8 mb-8">
      <h2 class="text-xl font-semibold mb-4">What is this?</h2>
      <p class="text-muted-foreground mb-4">
        A single file containing all the UX best practices from this site, formatted for AI assistants.
        When you provide this as context, your AI will automatically apply good UX patterns when generating UI components.
      </p>
      <p class="text-muted-foreground">
        Instead of asking <span class="text-foreground font-medium">"create a user list with skeleton loading"</span>,
        just ask <span class="text-foreground font-medium">"create a user list"</span> — the AI will know to use skeletons instead of spinners.
      </p>
    </div>

    <div class="rounded-xl border bg-card p-6 md:p-8 mb-8">
      <h2 class="text-xl font-semibold mb-4">How to use</h2>

      <div class="space-y-6">
        <div>
          <h3 class="font-medium mb-2 flex items-center gap-2">
            <span class="flex h-6 w-6 items-center justify-center rounded-full bg-primary text-primary-foreground text-sm font-bold">1</span>
            If your AI can read URLs
          </h3>
          <p class="text-muted-foreground text-sm mb-3 ml-8">
            Just give it the link. Works with Claude, ChatGPT with browsing, and similar tools.
          </p>
          <div class="ml-8">
            <CopyableUrl url="https://ux-good-patterns.netlify.app/ux-rules.md" client:load />
          </div>
        </div>

        <div class="border-t pt-6">
          <h3 class="font-medium mb-2 flex items-center gap-2">
            <span class="flex h-6 w-6 items-center justify-center rounded-full bg-primary text-primary-foreground text-sm font-bold">2</span>
            If you need to paste the content
          </h3>
          <p class="text-muted-foreground text-sm mb-3 ml-8">
            Copy the full content and paste it in your conversation or project context (Cursor rules, Claude project, etc.)
          </p>
        </div>

        <div class="border-t pt-6">
          <h3 class="font-medium mb-2 flex items-center gap-2">
            <span class="flex h-6 w-6 items-center justify-center rounded-full bg-primary text-primary-foreground text-sm font-bold">3</span>
            If you prefer a local file
          </h3>
          <p class="text-muted-foreground text-sm mb-3 ml-8">
            Download the file and add it to your project's context (e.g., <code class="rounded bg-muted px-1.5 py-0.5">.cursorrules</code>, <code class="rounded bg-muted px-1.5 py-0.5">CLAUDE.md</code>, etc.)
          </p>
        </div>
      </div>
    </div>

    <AiContextActions client:load />

    <div class="rounded-xl border border-dashed bg-muted/30 p-6 text-center">
      <p class="text-sm text-muted-foreground">
        The file is auto-generated from all patterns and articles on this site.<br />
        It updates automatically when new content is added.
      </p>
    </div>
  </div>
</PageLayout>
