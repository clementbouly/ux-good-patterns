---
import { ExampleGrid } from "./ExampleGrid";
import { getLangFromLocale, type Lang } from "@/i18n";

const lang = getLangFromLocale(Astro.currentLocale) as Lang;
---

<ExampleGrid client:load lang={lang} />
